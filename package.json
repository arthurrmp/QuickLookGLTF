{
  "name": "glbquicklook",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/entry.js --bundle --format=esm --outfile=GLTFPreview/bundle.js && node scripts/copy-decoders.js",
    "watch": "esbuild src/entry.js --bundle --format=esm --outfile=GLTFPreview/bundle.js --watch"
  },
  "dependencies": {
    "three": "^0.183.2"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
